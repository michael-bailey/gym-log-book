apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gym-log-book
spec:
  secretName: gym-log-book-tls
  dnsNames:
    - gym.example.com
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-prod
