apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gym-log-book

resources:
  - ../../base
  - ingress.yaml

patches:
  - path: rollout-patches.yaml
  - path: image-patches.yaml
